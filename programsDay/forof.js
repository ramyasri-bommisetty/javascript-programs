

// ## **Day 1 – Basics with Arrays**

// 1. Create an array of numbers `[1, 2, 3, 4, 5]`. Use `for...of` to print each number.
// 2. Multiply each number by 2 and print the result.

let numbers =[1,2,3,4,5];
for(const num of numbers){
    
    console.log(num * 2);
}

// ---

// ## **Day 2 – Strings**

// 1. Take a string `"hello"`. Print each character.
// 2. Count and print the number of vowels in the string.

let string ="hello";
let vowels;
if(vowels ='a'||'e'||'i'||'o'||'u')
{
for (let vowels of string ){
    console.log(vowels);
}

}
// ---

// ## **Day 3 – Conditional inside loop**

// 1. Create an array of numbers `[10, 15, 20, 25, 30]`. Print only numbers greater than 20.
// 2. Create an array of words `["apple", "banana", "kiwi"]` and print words longer than 4 letters.

// ---

// ## **Day 4 – Working with Sets**

// 1. Create a `Set` of fruits: `["apple", "banana", "mango"]`. Print each fruit.
// 2. Print only fruits that contain the letter `"a"`.

// ---

// ## **Day 5 – Maps**

// 1. Create a `Map` of students and scores: `{"Alice": 90, "Bob": 75, "Charlie": 85}`. Print all names and scores.
// 2. Print the names of students who scored above 80.

// ---

// ## **Day 6 – Nested arrays (matrix)**

// 1. Create a matrix: `[[1,2],[3,4],[5,6]]`. Print all numbers using nested `for...of`.
// 2. Sum all numbers in the matrix.

// ---

// ## **Day 7 – Functions & arguments**

// 1. Write a function `sumAll(...nums)` that sums any number of arguments using `for...of`.
// 2. Write a function `maxNumber(...nums)` that finds the maximum number using `for...of`.

// ---

// ## **Day 8 – Strings & conditions**

// 1. Given a string `"JavaScript"`, print only uppercase letters.
// 2. Reverse a string using `for...of`.

// ---

// ## **Day 9 – Combine arrays & maps**

// 1. Create an array of objects:

// ```js
// const students = [
//   { name: "Alice", score: 90 },
//   { name: "Bob", score: 75 },
//   { name: "Charlie", score: 85 }
// ];
// ```

// Use `for...of` to print names of students with score > 80.

// 2. Calculate the average score of all students.

// ---

// ## **Day 10 – Advanced Practice**

// 1. Flatten a 2D array using `for...of`:

// ```js
// const matrix = [[1,2],[3,4],[5,6]];
// ```

// Output: `[1,2,3,4,5,6]`.
// 2. Count how many times each character appears in a string using `for...of`.

// ---

